# Changelog

## 1.0.0

- Initial release: `gh shieldly` — thin wrapper running `@shieldly/cli` via npx.
  All CLI commands and flags pass through (`analyze-iam`, `analyze-cf`,
  `agent-scan`, `--help`, ...).
